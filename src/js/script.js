window.addEventListener("DOMContentLoaded", () => {
  const tabs = require("./Components/tabs");
  const slider = require("./Components/slider");
  const calcRation = require("./Components/calcRation")
  tabs();
  slider();
  calcRation();
});
